<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Purple theme (dark) -->
    <style name="Theme.TelegramNewsReader.Purple" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="actionBarStyle">@style/CustomActionBarStyle</item>
    </style>

    <!-- Teal theme (dark) -->
    <style name="Theme.TelegramNewsReader.Teal" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/teal_200</item>
        <item name="colorPrimaryVariant">@color/teal_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="actionBarStyle">@style/CustomActionBarStyle</item>
    </style>

    <!-- Light theme -->
    <style name="Theme.TelegramNewsReader.Light" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/light_primary</item>
        <item name="colorPrimaryVariant">@color/light_primary_variant</item>
        <item name="colorOnPrimary">@color/light_on_primary</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="actionBarStyle">@style/CustomActionBarStyle</item>
    </style>

    <!-- Main theme reference -->
    <style name="Theme.TelegramNewsReader" parent="Theme.TelegramNewsReader.Purple" />

    <!-- Стиль для ActionBar -->
    <style name="CustomActionBarStyle" parent="Widget.Material3.ActionBar.Solid">
        <item name="titleTextStyle">@style/CustomActionBarTitle</item>
        <item name="android:titleTextStyle">@style/CustomActionBarTitle</item>
    </style>

    <!-- Стиль для заголовка ActionBar -->
    <style name="CustomActionBarTitle" parent="TextAppearance.Material3.ActionBar.Title">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <!-- Стиль для кнопок настроек -->
    <style name="SettingsButton" parent="Widget.Material3.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <!-- Стиль для элементов списка в настройках -->
    <style name="SelectableItemStyle">
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
    </style>
</resources>
